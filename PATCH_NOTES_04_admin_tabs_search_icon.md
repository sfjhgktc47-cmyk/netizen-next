# Patch 04 — admin tabs and search icon

Изменения:

1. Исправлены вкладки в админке на странице `/nz-console/products`:
   - вкладки `Карточки товаров / Отзывы / Вопросы` теперь открываются обычной ссылкой `<a>`, без клиентского `next/link`-перехода;
   - вкладки статусов карточек тоже переведены на обычные ссылки;
   - это убирает баг, когда URL менялся с первого клика, а контент открывался только со второго.

2. Исправлена иконка поиска:
   - убран светлый фон внутри SVG;
   - сама лупа стала крупнее;
   - кнопка поиска больше не рисует дополнительный фон вокруг иконки.

Файлы:
- `src/app/nz-console/products/page.tsx`
- `src/components/site-header.tsx`
- `public/icons/search-blue.svg`
