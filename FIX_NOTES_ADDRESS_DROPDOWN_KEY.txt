Исправлено:
- большой чёрный блок «Ищем варианты» больше не появляется;
- статус поиска показывается маленькой строкой под полем;
- список открывается только когда реально есть варианты;
- список использует цвета текущей темы;
- колонка города расширена, поэтому названия не сжимаются;
- добавлены DADATA_API_KEY и YANDEX_GEOSUGGEST_API_KEY в .env.example.
