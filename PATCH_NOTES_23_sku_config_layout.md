# PATCH 23 — SKU configuration layout fix

- Исправлен блок «Конфигурация» на создании SKU.
- Память и SIM стоят в верхнем ряду.
- Цвет вынесен отдельной строкой внутри секции, поэтому кнопки «Пип.» и «Сохр.» больше не налезают на SIM.
- Блок цвета получил отдельную рамку и фон, чтобы визуально не слипался с остальными полями.
- Логика сохранения цвета не менялась.
