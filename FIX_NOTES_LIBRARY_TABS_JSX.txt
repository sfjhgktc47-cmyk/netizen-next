Исправлена JSX-структура вкладок библиотеки контента.

Причина ошибки:
- условие tab === "benefits" случайно попало внутрь selectedBanner;
- после этого появился второй лишний `) : (`;
- JSX-парсер останавливался на строке 269.

Теперь структура корректная:
- banners -> редактор баннеров;
- benefits -> редактор преимуществ магазина;
- service -> преимущества сервиса и поддержки.
