Фикс блока преимуществ на главной.

Что изменено:
- блок преимуществ больше не привязан к сетке из 5 колонок;
- карточки преимуществ автоматически центрируются при любом количестве: 1, 2, 3, 4, 5+;
- если преимуществ меньше 5, справа больше не остаётся пустая колонка;
- текст заголовка и описания больше не обрезается через line-clamp;
- карточки становятся выше по содержимому, чтобы весь текст был виден;
- на мобильных карточки переносятся аккуратно, без горизонтального скролла.

Изменён файл:
- src/app/home-client.tsx
